<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/brown</item> <!--Barra superior -->
        <item name="colorPrimaryDark">@color/brown</item> <!--Notificaciones -->
        <item name="colorAccent">@color/barra</item> <!--Para resaltar -->
        <!--<item name="android:textColor">@color/black</item> Intento de cambiar el color de la letra -->
        <!--<item name="ActionBarStyle">@style/ActionBarStyle1</item> -->
    </style>
    <!--
        <style name="ActionBarStyle1" parent="Widget.AppCompat.ActionBar.TabText">
            <item name="android:textColor">@color/black</item>
            <item name="android:titleTextStyle">@style/ActionBarTitleTextStyle</item>
            <item name="android:background">@color/white</item>
        </style>
        <style name="ActionBarTitleTextStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
            <item name="android:textColor">@color/black</item>
        </style>-->
</resources>