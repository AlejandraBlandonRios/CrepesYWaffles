<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/barra</item> <!--Barra superior -->
        <item name="colorPrimaryDark">@color/barra</item> <!--Notificaciones -->
        <item name="colorAccent">@color/barra</item> <!--Para resaltar -->
        <item name="android:textColor">@color/black</item> <!--Intento de cambiar el color de la letra -->
    </style>

</resources>
