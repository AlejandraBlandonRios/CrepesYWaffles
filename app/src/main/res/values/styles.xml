<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item> <!--Barra superior -->
        <item name="colorPrimaryDark">@color/black</item> <!--Notificaciones -->
        <item name="colorAccent">@color/brown</item>
        <item name="android:textColor">@color/black</item> <!--Intento de cambiar el color de la letra -->
    </style>

</resources>
